# Normalize line endings: store text as LF in the repo, check out native.
# Keeps diffs clean across the team (Windows dev + Linux/OCI deploy).
* text=auto eol=lf

# Binary assets — never line-ending convert.
*.pdf  binary
*.png  binary
*.jpg  binary
*.jpeg binary
*.gif  binary
*.ico  binary
